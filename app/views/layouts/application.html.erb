<!DOCTYPE html>
<html lang="en">
<head>
  <title>DevtestLocaweb</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>

  <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no">
</head>
<body>

<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to root_path, class: "navbar-brand" do %>
      	DevTest
      <% end -%>
    </div>

    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <% if user_signed_in? -%>
        <ul class="nav navbar-nav">
          <li><%= link_to "Ocorrências", occurrences_url %></li>
          <li><%= link_to "Usuários", users_url %></li>
        </ul>
      <% end -%>

      <ul class="nav navbar-nav pull-right">
        <li>          
          <div id="user_nav">
            <% if user_signed_in? %>
              Logado como <%= current_user.email %> - <%= current_user.name %>
              <%= link_to "Sair", destroy_user_session_url, :method => :delete , class: 'btn btn-danger navbar-btn' %>
            <% else %>
              <%= link_to "Entrar", new_user_session_url, class: 'btn btn-default navbar-btn' %>
            <% end %>
          </div>
        </li>
      </ul>
    </div>
  </div>
</nav>

<div class="container-fluid">
  <% flash.each do |key, msg| -%>
    <p class="<%= key %>"> <%= msg %> </p>
  <% end -%>
  
  <%= yield %>
</div>

</body>
</html>
